<?php

// post جدول(read (select all , select one) 

// , insert (images)

// , delete , update (unlink)) 

// users (register , login) authantication

// pagination (limte) offset عدد الصفحات بنعمله ف صفحه ال ui

// authorization (permison) user admin مين اللي يدخل وادد ويمسح ويعدل ومين اللي مسمحلوه انه يشوف بس
// admin (without register form) superadmin add admis (register) --> login
 
// localization الترجمه
 
